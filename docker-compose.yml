version: "3"

services:
  app:
    build:
      context: .
    # entrypoint: ./docker-entrypoint.sh
    env_file: .env
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./app:/app
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"
